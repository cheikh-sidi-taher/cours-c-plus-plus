#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double number;
    cout << "Enter the  number: ";
    cin >> number;
    cout << "Power of the number is: " << round(pow(number,4)) << endl;
    cout << "Power of the number is: " << round(pow(number,4)) << endl;
    cout << "Power of the number is " <<  round(pow(number,4)) << endl;
    return 0;
}